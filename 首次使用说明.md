# 瀟湘館首次使用

1. 安装 Node.js 20+、Python 3，运行 python -m pip install -r requirements.txt。
2. 运行 npm start；Windows 也可双击 start-library.cmd。打开 http://127.0.0.1:4327/ 。
3. 打开“API 设置”，填写自己的翻译服务商和 API Key，并测试连接。
4. 导入无 DRM 的 EPUB、PDF 或 AZW3，选择原文语种，识别章节并开始翻译。
5. 扫描 PDF 另需 Tesseract 和相应语言模型；详细配置见 README.md。

首次启动为空书库。你的书籍、译文和密钥保存在设置页显示的本机数据目录，关闭浏览器不会删除它们。迁移或备份时先停止服务，再复制整个数据目录。
